# Disable banner
spring.main.banner-mode=off

# Home directory with configuration
# Must be same value as TestConstants.WORK_SBS_SHARED
home.dir=target/sbs_shared

# Root context path
server.servlet.context-path=/web_test

# Default configuration parameters for rest_cfg
rest.config.debug=false

# Default configuration parameters for web_cfg
ui.config.lang=en

# logging.level.Test=debug
# logging.level.root=info
# logging.level.org.springframework.web=info
